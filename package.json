{
  "name": "mailer",
  "private": true,
  "version": "1.0.0",
  "description": "Mini-Brevo prospect dashboard with OpenAI personalization",
  "scripts": {
    "dev": "npm run dev:backend & npm run dev:admin",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:admin": "npm run dev --prefix admin",
    "build": "npm run build --prefix backend && npm run build --prefix admin",
    "start:backend": "npm run start:prod --prefix backend"
  }
}
